# Poké Project

A Pokédex-like web application for your Pokémon catching needs.

To start the application, run the following `pixi` command and visit http://localhost:8080 in your web browser.

```sh
pixi run app
```
